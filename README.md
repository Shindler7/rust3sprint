# Блог-платформа

Учебный проект: платформа для ведения блога, полностью написанная на Rust,
включая фронт-часть. Создано в качестве практического задания одного из модулей
курса "Rust для действующих разработчиков" в Яндекс.Практикуме.

Проект объединяет в одном решении технологии по принципу "шампуня 4 в 1":
веб-сервер с HTTP и gRPC API, клиентскую библиотеку, CLI-инструмент
и WASM-фронтенд.

## Структура проекта

Рабочее пространство (`workspace`) состоит из 5 ящиков (крейтов), из них
2 бинарных приложения (`bin`) и 3 библиотеки (`lib`):

- `blog-server` — консольный HTTP и gRPC сервера, которые запускаются в разных
  потоках и прослушивают разные порты. Ядро проекта, обеспечивающее
  функциональность API блог-платформы, включая взаимодействие с базой данных
- `blog-client` — библиотека, предоставляющая методы для взаимодействия с API
  блог-платформы (создание, редактирование, удаление, просмотр записей)
- `blog-cli` — CLI-адаптер для взаимодействия с блог-платформой. С помощью
  консольных команд позволяет взаимодействовать с API.
- `blog-wasm` — WebAssemble-библиотека, обеспечивающая представление
  фронт-части блог-платформы в браузере
- `proto-crate` — библиотека, содержащая Protobuf-спецификации и
  сгенерированный Rust-код для сообщений и сервисов gRPC API блог-платформы.
  Обеспечивает единый контракт данных между сервером (`blog-server`),
  клиентской библиотекой (`blog-client`) и другими потребителями API.

## ✨ Возможности

- **Функциональный HTTP и gRPC сервера**: "из коробки" доступны быстро
  поднимаемые
  серверные решения, с набором базовых методов взаимодействия с API
- **Безопасность сессий**: хеширование паролей и предоставление JWT-токенов,
  на основе современных криптографических решений, созданных на Rust
- **Гибкая структура слоёв**: чёткое разделение ответственности компонентов
  в каждой коробке, с возможностью быстрой адаптации под новые требования
  (например, использование другой базы данных, вместо базовой `PostgreSQL`).
- **gRPC и Protobuf**: быстрая генерация Rust-кода на основе `.proto`-схем.
  Изменения в .proto-файлах автоматически распространяются на все зависимые
  коробки

## Установка

### Базовые требования к системе

- [Rust Toolchain](https://rust-lang.org/tools/install/) (Rust и Cargo, версия
  1.90+)
- [PostgreSQL](https://www.postgresql.org/download/) (версия 15+)
- Система контроля версий [Git](https://clck.ru/V9Qx2)
- Компилятор Protocol Buffer ([инструкция](https://protobuf.dev/installation/))

### Компиляция

Все приложения объединены в одно рабочее пространство (`workspace`)
и компилируются одновременно.

```shell
git clone git@github.com:Shindler7/rust3sprint.git
cd rust3sprint
cargo build --release
```

## 🚀 Быстрый старт

### Создание env

Необходимо создать `.env`-файл с основными настройками. Примерная структура
представлена ниже, более точную порекомендует само приложение при запуске без
файла или с неправильной конфигурацией.

```text
# Host
SERVER_HOST=127.0.0.1
SERVER_PORT=8080

# CORS.
CORS_URL=http://localhost:3000
CORS_MAX_AGE=600

# gRPC
GRPC_PORT=50051

# Database.
DB_USERNAME=<your_username_here>
DB_PASSWORD=<your_password_here>
DB_HOST=127.0.0.1
DB_PORT=5432
DB_NAME=practice_db
DB_MAX_CONN=10

# Logging
RUST_LOG=blog-server=debug,actix_web=info
```

### Запуск сервера

В режиме разработки (debug):

```shell
cargo run --bin blog-server
```

Скомпилированное приложение:

```shell
./blog-server
```

**Важно**: если в системе не установлен компилятор `protoc` (см. выше "Базовые
требования"), запуск сервера завершится паникой. Потому что `.proto`-схемы
генерируются в код перед стартом приложения и не передаются через репозиторий.

### Запуск клиента

TODO!

## Основная функциональность

TODO!

## Документация

Код приложения документирован в пределах разумной достаточности, а также 
с учётом строгой типизации Rust, в контексте предоставляющей информацию 
о данных.

Чтобы сгенерировать документацию, можно использовать стандартный `cargo doc`. 
Например, так (из корня проекта или в каталоге отдельных крейтов):

```shell
cargo doc --open --message-format human --color auto --no-deps
```

## Тесты

Приложение содержит unit-тесты, покрывающие частично инфраструктуру проекта.

Запуск тестов в стандартном режиме:

```shell
cargo test
```

Для тестирования с предоставлением деталей:

```shell
cargo test -- --nocapture
```

## Благодарности

Команде "Яндекс Практикум".
